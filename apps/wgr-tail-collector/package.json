{
  "name": "@wgr/wgr-tail-collector",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Tail Worker — forwards trace events from other Workers to wgr-logs",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240821.1",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.0"
  }
}
